name: Test
run-name: ${{ github.actor }} is learning GitHub Actions

on: [push]

env:
  TEST: ${{ secrets.TOKEN }}

jobs:
  sandbox:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo $TEST
